apiVersion: v1
kind: Service
metadata:
  name: github-actions-demo-service
spec:
  type: NodePort
  selector:
    app: github-actions-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080